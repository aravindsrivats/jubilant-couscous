import { initialState as step } from './step'
import { initialState as pan } from './pan'
import { initialState as bank } from './bank'

const initialState = {
  step,
  pan,
  bank,
}

export { initialState }